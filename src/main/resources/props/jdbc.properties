# Properties file with JDBC and JPA settings.
#
# Applied by <context:property-placeholder location="jdbc.properties"/> from
# various application context XML files (e.g., "applicationContext-*.xml").
# Targeted at system administrators, to avoid touching the context XML files.

# Properties that control the population of schema and data for a new data source

cur.jdbc.driverClassName=com.mysql.jdbc.Driver
cur.jdbc.url=jdbc:mysql://127.0.0.1/sell?characterEncoding=utf-8&useSSL=false
cur.jdbc.username=root
cur.jdbc.password=root123

his.jdbc.driverClassName=com.mysql.jdbc.Driver
his.jdbc.url=jdbc:mysql://127.0.0.1/hissell?characterEncoding=utf-8&useSSL=false
his.jdbc.username=root
his.jdbc.password=root123
